# Задание 3.3: Сортировка и фильтрация

## 🎯 Це��ь

Реализовать сортировку, фильтрацию и выбор полей (sparse fieldsets) для REST API запросов.

## Требования

1. Реализуйте сортировку через `?sort=-createdAt,name` (минус = DESC, без минуса = ASC)
2. Реализуйте фильтрацию: `?filter[status]=active`, `?filter[age][gte]=18`, `?filter[role][in]=admin,editor`
3. Реализуйте текстовый п��иск: `?search=john` -> ILIKE по нескольким полям
4. Реализуйте sparse fieldsets: `?fields=id,name,email` -> SELECT только указанных полей
5. Покажите комбинированный запрос со всеми параметрами

## Чек��ист

- [ ] Сортировка: поддержка нескольких полей, ASC/DESC через знак минус
- [ ] Фильтрация: операторы gte, lte, in, like через вложенные query params
- [ ] Поиск: ILIKE по нескольким полям одновременно
- [ ] Sparse fieldsets: SELECT только запрошенных полей
- [ ] Комбинированный пример: filter + sort + fields + page в одном запросе

## Как проверить себя

Нажмите "Запу��тить" и убедитесь, что каждый тип параметров показан с примером query string и соответствующим SQL-запросом, а комбинированный пример объединяет все параметры.
