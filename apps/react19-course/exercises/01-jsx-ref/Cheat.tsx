// ============================================
// Подсказки к уровню 1: JSX Transform & ref as prop
// ============================================

// --- Задание 1.1: JSX без import React ---
// - Начиная с React 17, новый JSX-трансформ не требует `import React from 'react'`
// - В React 19 этот трансформ обязателен
// - Просто удалите строку `import React from 'react'`
// - Импортируйте только то, что используете: `import { useState } from 'react'`
// - Убедитесь, что сборщик (Vite, webpack) настроен на новый JSX-трансформ

// --- Задание 1.2: ref как prop ---
// - В React 19 `ref` — обычный prop, `forwardRef` больше не нужен
// - Определите компонент: `function FancyInput({ ref, ...props })`
// - Передайте ref внутреннему элементу: `<input ref={ref} />`
// - Используйте как обычно: `<FancyInput ref={myRef} />`

// --- Задание 1.3: ref cleanup function ---
// - В React 19 ref-колбэки могут возвращать функцию очистки
// - Паттерн: `ref={(node) => { /* настройка */; return () => { /* очистка */ } }}`
// - Функция очистки вызывается при удалении элемента из DOM
// - Это заменяет старый паттерн проверки `if (node === null)`

// --- Задание 1.4: Убрать forwardRef ---
// - Удалите обёртку `forwardRef`
// - Перенесите `ref` в деструктуризацию props
// - Было: `const Comp = forwardRef((props, ref) => ...)`
// - Стало: `function Comp({ ref, ...props }) { ... }`
// - `displayName` больше не нужен при использовании именованных функций

export {}
