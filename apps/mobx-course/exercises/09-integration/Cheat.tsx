// 9.1: toJSON() + autorun(() => localStorage.setItem(key, JSON.stringify(store.toJSON())))
// 9.2: Test: const store = new Store(); store.action(); assert(store.computed === expected)
// 9.3: Kanban: columns[], addCard, moveCard, removeCard
export {}
