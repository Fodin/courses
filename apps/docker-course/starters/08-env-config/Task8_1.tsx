// Задание 8.1: ENV и .env файлы
// Task 8.1: ENV and .env files
//
// TODO: Покажите способы передачи переменных окружения: ENV в Dockerfile, ARG vs ENV, флаг -e, .env файлы, подстановка в Compose
// TODO: Show environment variable mechanisms: ENV in Dockerfile, ARG vs ENV, -e flag, .env files, Compose substitution
//
// 1. Пять переключаемых секций с блоком кода и ключевыми моментами для каждой
// 2. Кнопка для показа таблицы приоритетов переменных (5 уровней)
// 3. Сводка синтаксиса подстановки: ${VAR:-default}, ${VAR:?error}, ${VAR:+alt}, ${VAR}

export function Task8_1() {
  return (
    <div style={{ padding: '1rem' }}>
      <h3>Задание 8.1: ENV и .env файлы</h3>
      <p>Реализуйте компонент с обзором механизмов передачи переменных окружения...</p>
    </div>
  )
}
