// Задание 1.3: docker build
// Task 1.3: docker build
//
// TODO: Покажите команду docker build с основными флагами.
// TODO: Show docker build command with key flags.
//
// 1. Основные флаги: -t, -f, --no-cache, --build-arg, --target
//    Key flags: -t, -f, --no-cache, --build-arg, --target
// 2. Объясните build context и .dockerignore
//    Explain build context and .dockerignore
// 3. Покажите пример вывода сборки
//    Show an example build output

export function Task1_3() {
  return (
    <div style={{ padding: '1rem' }}>
      <h3>Задание 1.3: docker build</h3>
      <p>Реализуйте компонент с описанием процесса сборки...</p>
    </div>
  )
}
