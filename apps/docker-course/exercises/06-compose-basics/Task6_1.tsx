// Задание 6.1: Структура docker-compose.yml
// Task 6.1: docker-compose.yml structure
//
// TODO: Покажите корневые секции файла, полный пример и объясните deprecated version
// TODO: Show root sections of the file, full example and explain deprecated version
//
// 1. Три секции: services (обязательная), volumes, networks -- с описанием и YAML
// 2. Полный пример docker-compose.yml (web + api + db, 2 сети, 1 том)
// 3. Сравнение старого (version: '3.8') и нового формата

export function Task6_1() {
  return (
    <div style={{ padding: '1rem' }}>
      <h3>Задание 6.1: Структура docker-compose.yml</h3>
      <p>Реализуйте компонент с обзором структуры docker-compose.yml...</p>
    </div>
  )
}
