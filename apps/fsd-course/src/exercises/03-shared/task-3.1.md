# Задание 3.1 — Public API переиспользуемого компонента (простое)

## Цель

Закрыть сегмент `shared/ui` публичным входом — файлом `index.ts`.

## Что дано

- `shared/ui/Button.tsx` — готовый компонент `Button` и тип его пропов
  `ButtonProps` (только чтение).
- `shared/ui/index.ts` — пустой, с TODO-комментарием.

## Требования

1. В `shared/ui/index.ts` реэкспортируйте компонент `Button`.
2. Реэкспортируйте тип `ButtonProps`.
3. Нажмите «Проверить».

## Чеклист

- [ ] `shared/ui/index.ts` реэкспортирует `Button`
- [ ] `shared/ui/index.ts` реэкспортирует `ButtonProps`
- [ ] Пройти квиз уровня ≥ 80%
