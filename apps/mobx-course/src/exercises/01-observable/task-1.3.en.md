# Task 1.3: Observable Types

## Goal
Work with different collection types: array, Map, Set.

## Requirements

- [ ] Create `CollectionStore` with `items: string[]`, `tags: Map`, `selectedIds: Set`
- [ ] Add `addItem`, `removeItem` methods for array
- [ ] Add `setTag`, `removeTag` methods for Map
- [ ] Add `toggleId` method for Set
- [ ] Add computed `summary` — brief statistics
- [ ] Display all collections and control buttons in component
